# 2026-07-27 - Status correction + Sprint 5 (M6) kickoff

## What happened

Session opened by re-pulling the repo and cross-checking the cache
(`exploration-cache/`, dated 2026-07-10) against actual code. Found the cache
was under-reporting real progress:

- **Sprint 3 (M4 - Demande Formelle)**: cache said "kickoff." Repo shows API +
  admin + portal fully committed.
- **Sprint 4 (M5 - Évaluation Approfondie)**: cache said "kickoff in progress,"
  a prior chat handoff claimed the 17 admin + 2 portal frontend files were
  "written but not yet applied." Direct inspection found all 19 files present
  and committed (`bf806d7`). Fresh `tsc --noEmit` on `apps/api`, `apps/admin`,
  `apps/portal` - all clean, zero errors. Fred independently confirmed via
  live app testing (admin + portal M5 flows work).

Also checked Notion (dashboard page + Backlog DB):

- Dashboard page (`3950cc37-...`) still shows Sprint 2 as "À démarrer" -
  stale since ~2026-07-07, never updated past Sprint 1.
- Backlog DB: every M3/M4/M5/M6/M7 row still `Not started`, `createdTime`
  2026-07-07, never touched since creation.

Both were resynced this session (see Notion directly - dashboard status table
and backlog `Statut` field updated for all M3/M4/M5 rows to `Done`, dashboard
next-action updated to Sprint 5/M6).

## Certificate templates (for future M7)

Fred added `docs/CERTIFICAT_DAGREMENT_RAG-5_3.docx` and
`docs/CERTIFICAT_DE_RECONNAISSANCE_DAGREMENT_RAG-5_3.docx` (commit `7935911`).
Reviewed both: same layout, only the title block differs
(agrément vs. reconnaissance d'agrément) - confirms the two-value
`certificate_type` enum already in schema is correct and sufficient. Fields
present: reference number, org name/address/phone/email, expiration date,
Classe/Qualification/Limites table (variable row count), initial vs. current
issue date, DG signatory name (likely a `system_parameter`, not per-record).
Relevant when Sprint 6 starts - not acted on yet.

## Decision: r3_agent auth (M6)

Confirmed with Fred: `r3_agent` does **not** need a separate login system.
Reuses the existing staff `users`/`user_roles` auth (same as `s5_agent`,
`dn_agent`, etc.) - just a role-filtered dossier queue in admin, same pattern
as every other internal role.

## Started this session

Sprint 5 (M6 - Démonstration/Inspection sur Site), API first. See
`active-session/current-task.md` and `active-session/next-actions.md` for
scope and sequencing.

## Lesson for future sessions

This cache is only as good as its last sync. When it disagrees with the repo
tree or a typecheck, **the repo wins** - inspect the actual code before
reporting status, don't just read the cache. Update this cache at the end of
every session, not just when convenient.

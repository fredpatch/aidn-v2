# Session — 2026-07-09

Follow-up session after Sprint 2 commit (`0e29d06`), focused on hardening and operational controls.

## What was done

- Added a new admin settings area:
  - route wired in admin app (`/parametres`)
  - new page `SettingsPage` for SU users
- Surfaced system parameters management end to end:
  - API route/controller for `GET /api/system-parameters` and `PATCH /api/system-parameters/:key`
  - mounted in server
  - admin UI can list/edit grouped parameters
- Added dev-only data reset tooling:
  - new `dev-tools` module (`status` + scoped `reset`)
  - guarded by SU role and `ENABLE_DEV_RESET=true`
  - scoped cleanup for requests/workflow, organisations/applicants, notifications, audit logs, reports
- Completed meeting compte-rendu flow:
  - added `POST /api/meetings/:id/report`
  - service persists report via `document_versions` and updates meeting CR fields
  - portal now displays CR link when available
  - admin phase page now supports CR upload after meeting is held
- Tightened M3 closure logic:
  - phase cannot close while meeting is unresolved (`scheduled`)
  - phase cannot close until preliminary declaration is submitted
  - closure note/document remain optional
- Aligned preliminary declaration submission with document versioning pattern
  (`document_versions`, replace marks old version as trashed)
- Minor admin UX polish:
  - added `in_progress` badge color in requests list

- Refactored M3 admin page for maintainability:
  - split `PreliminaryPhasePage.tsx` into module-scoped files under
    `pages/phases/preliminary/` (types/constants/helpers/api/hooks/components)
  - extracted reusable status badge component (`PhaseStatusBadge`)
  - migrated domain hooks to React Query (`useQuery`/`useMutation`) with
    invalidation-driven refresh instead of manual reload callbacks
  - centralized query keys in `src/lib/react-query/queryKeys.ts`
  - moved M3 api/types into shared lib layer (`src/lib/api/preliminary.api.ts`,
    `src/lib/api/preliminary.types.ts`)
  - wired global QueryClient in `src/main.tsx` (+ React Query devtools)
  - added Zustand baseline store (`src/lib/stores/ui.store.ts`) for future
    client-state use without mixing with server-state
  - added helper test scaffold (`helpers.test.ts`) with
    `runPreliminaryHelpersTests`
- Rolled out the same frontend data-layer convention to auth/settings domains:
  - migrated `useAuth` to React Query-backed state (`me` + bootstrap status)
  - moved auth HTTP calls to `src/lib/api/auth.api.ts` (`LoginPage` and
    `BootstrapPage` now consume API helpers)
  - moved settings/dev-tools calls to `src/lib/api/settings.api.ts`
  - added `useSystemParameters` + `useDevReset` hooks and query-key
    invalidation for settings page workflows
  - expanded query key registry to include `auth` and `settings`
- Added shadcn Sonner notifications across both frontends:
  - mounted global toaster in admin + portal roots
  - added `notify` helpers in both apps
  - connected login/settings/request actions to success/error/warning toasts
- Planned (not executed in this pass):
  - dedicated portal refactor to adopt admin's React Query + shared `lib/api`
    architecture for portal auth/request/preliminary flows
- Sprint 3 backend kickoff (M4 formal request):
  - added new API module `formal-request` and mounted `/api/formal-request`
  - implemented phase opening gate (M3 must be closed) and bundle endpoint by request
  - implemented formal letter DG circuit transitions (`submitted` -> `signed` -> `pending_review`)
  - implemented 11-slot formal document checklist uploads with version/trash behavior
  - implemented M4 closure gate checks (letter transmitted + all slots submitted + meeting resolved)
  - added formal-request error handler mapping and shared formal slot constants
- Sprint 3 admin frontend kickoff (M4 formal phase):
  - added admin route `/demandes/:requestId/phase-formelle`
  - created formal-phase feature module (`pages/phases/formal/*`) with cards/hooks/helpers
  - added formal API/types client layer in `apps/admin/src/lib/api/formal.*`
  - added React Query formal keys and invalidation-based flows
  - updated requests page to expose M4 navigation alongside M3 for in-progress dossiers
- Sprint 3 portal frontend incremental pass (M4):
  - updated `MyRequestPage.tsx` with improved French labels/statuses and centralized API origin links
  - added initial portal `FormalPhaseSection` for M4 (letter submission, document-slot submissions, formal meeting visibility)
  - kept this pass page-local; full modular React Query refactor remains scheduled

## Notes

- Sprint 2 remains fully complete and committed; this is a hardening layer on top.
- No new hard blockers identified.
- Browser rendering QA still requires manual verification outside this sandbox.

## Next

1. Commit and push the current diff including M4 admin frontend kickoff on top of backend APIs.
2. Add `PORTAL_ORIGIN` to `apps/api/.env.example`.
3. Continue Sprint 3 with end-to-end admin wiring, then portal M4 UI.

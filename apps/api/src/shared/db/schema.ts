/**
 * Drizzle schema - placeholder.
 *
 * The actual table definitions (requests, applicants, organisations, phases,
 * documents, payments, certificates, meetings, notifications, users/roles)
 * are the next task after scaffolding - see docs/TASKS.md Sprint 0,
 * "Schema PostgreSQL initial", and project/modules-feasibility.md for the
 * locked business rules each table must enforce.
 */
export {};
